# SSHield - ابزار امنیتی سرور لینوکس

SSHield یک ابزار امنیتی قدرتمند و جامع برای سرورهای لینوکس است که برای محافظت در برابر حملات brute-force و مدیریت قوی سرویس‌های حیاتی طراحی شده است. با یک منوی CLI کاربرپسند، SSHield امنیت سرور را ساده می‌کند و آن را برای مدیران جدید و باتجربه قابل دسترس می‌سازد.

## ⚙️ نصب

برای نصب SSHield، به سادگی دستور زیر را به عنوان کاربر root اجرا کنید:

```bash
curl -sSL https://raw.githubusercontent.com/jules-dot-ai/ops-sshield/main/install.sh | bash
```

نصب‌کننده تمام وابستگی‌ها را مدیریت کرده و اجزای لازم را تنظیم می‌کند.

## 🕹️ استفاده

پس از نصب، می‌توانید منوی SSHield را با تایپ کردن دستور زیر راه‌اندازی کنید:

```bash
sshield
```

این دستور یک منوی CLI تمیز و شهودی را باز می‌کند که در آن می‌توانید به تمام ویژگی‌های SSHield دسترسی داشته باشید.

## 🚀 ویژگی‌ها

### مدیریت سرویس‌ها
- **فعال/غیرفعال کردن سرویس‌ها**: به سرعت سرویس‌های حیاتی مانند `sshd`، `vsftpd` و `telnet` را فعال یا غیرفعال کنید.
- **بررسی وضعیت**: وضعیت فعلی (فعال/غیرفعال، فعال شده/غیرفعال شده) هر سرویس مدیریت شده را فوراً مشاهده کنید.
- **اول ایمنی**: این ابزار از غیرفعال کردن سرویس SSH که در حال حاضر از آن استفاده می‌کنید، جلوگیری می‌کند تا از قفل شدن تصادفی جلوگیری شود.

### مدیریت پورت‌ها
- **تغییر پورت SSH**: به راحتی پورت پیش‌فرض SSH را به یک پورت سفارشی تغییر دهید.
- **بررسی‌های امنیتی**: این ابزار به طور خودکار تداخل پورت‌ها را بررسی کرده و در مورد استفاده از پورت‌های ممتاز یا ناامن هشدار می‌دهد.
- **ادغام با فایروال**: قوانین `ufw` به طور خودکار برای اجازه دادن به پورت جدید و مسدود کردن پورت قدیمی به‌روز می‌شوند تا انتقال به آرامی انجام شود.

### محدودیت IP
- **اجازه/رد کردن IPها**: دسترسی به SSH را با ایجاد قوانین `ufw` برای اجازه دادن یا رد کردن آدرس‌های IP خاص محدود کنید.
- **اجازه خودکار IP فعلی**: برای جلوگیری از قفل شدن تصادفی، SSHield پیشنهاد می‌دهد که اگر هیچ قانون اجازه‌ای تشخیص داده نشود، آدرس IP فعلی شما را به طور خودکار مجاز کند.
- **مشاهده قوانین**: شما می‌توانید تمام قوانین فعال `ufw` را مستقیماً از منو مشاهده کنید.

### حفاظت در برابر حملات Brute-Force
- **ادغام با fail2ban**: SSHield نصب و پیکربندی `fail2ban` را به صورت خودکار انجام می‌دهد.
- **حفاظت از SSH**: یک jail `fail2ban` برای `sshd` به طور خودکار فعال می‌شود تا تلاش‌های ورود مخرب را نظارت و مسدود کند.
- **نظارت بر وضعیت**: شما می‌توانید وضعیت jail `sshd` را در هر زمان مشاهده کنید تا لیستی از IP‌های مسدود شده را ببینید.

### Port Knocking
- **مخفی کردن پورت SSH**: با پیاده‌سازی port knocking پورت SSH خود را از دید عموم مخفی کنید.
- **دنباله‌های سفارشی**: یک دنباله سفارشی از پورت‌ها را تعریف کنید که باید قبل از باز شدن پورت اصلی SSH "کوبیده" شوند.
- **پیکربندی خودکار**: SSHield نصب و پیکربندی `knockd` را انجام داده و قوانین فایروال لازم را تنظیم می‌کند.

### گزارش‌گیری و لاگ
- **اعلان‌های وب‌هوک**: یک URL وب‌هوک را برای دریافت اعلان‌های آنی برای هر ورود موفق SSH پیکربندی کنید. اعلان به صورت یک پیلود JSON با ساختار زیر ارسال می‌شود:
  ```json
  {
    "hostname": "your-server-hostname",
    "user": "the-user-logging-in",
    "source_ip": "the-source-ip-address",
    "timestamp": "YYYY-MM-DD HH:MM:SS",
    "message": "Full notification message"
  }
  ```
- **گزارش تلاش‌های ناموفق**: لیستی تجمیعی از تلاش‌های ورود ناموفق را بر اساس آدرس IP مشاهده کنید تا به راحتی تهدیدات مداوم را شناسایی کنید.

### خلاصه‌های امنیتی
- **گزارش‌های روزانه و هفتگی**: خلاصه‌های امنیتی بر اساس تقاضا برای ۲۴ ساعت یا ۷ روز گذشته تولید کنید.
- **معیارهای کلیدی**: خلاصه‌ها شامل ورودهای موفق، تلاش‌های ناموفق و لیستی از IP‌های مسدود شده توسط `fail2ban` است که یک نمای کلی سریع از وضعیت امنیتی سرور شما ارائه می‌دهد.
