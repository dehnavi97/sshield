# SSHield - ابزار امنیتی سرور لینوکس

SSHield یک ابزار امنیتی قدرتمند و جامع برای سرورهای لینوکس است که برای محافظت در برابر حملات brute-force و مدیریت قوی سرویس‌های حیاتی طراحی شده است. با یک منوی CLI کاربرپسند، SSHield امنیت سرور را ساده می‌کند و آن را برای مدیران جدید و باتجربه قابل دسترس می‌سازد.

## 🚀 ویژگی‌ها

- **مدیریت سرویس‌ها**: به راحتی سرویس‌های ضروری مانند SSH، FTP و Telnet را فعال، غیرفعال و وضعیت آن‌ها را بررسی کنید.
- **مدیریت پورت‌ها**: پورت‌های سرویس را با بررسی‌های امنیتی و تداخل داخلی به صورت ایمن تغییر دهید.
- **محدودیت IP**: دسترسی به سرویس‌های خود را با اجازه دادن یا رد کردن آدرس‌های IP خاص با استفاده از `ufw` محدود کنید.
- **حفاظت در برابر حملات Brute-Force**: با `fail2ban` ادغام شده تا به طور خودکار IP‌های مشکوک را مسدود کند.
- **Port Knocking**: پورت SSH خود را از اسکنرها با پیاده‌سازی port knocking با `knockd` مخفی کنید.
- **گزارش‌گیری و لاگ**: اعلان‌های وب‌هوک آنی برای ورودهای موفق SSH دریافت کنید و گزارش‌های تجمیعی از تلاش‌های ناموفق را مشاهده کنید.
- **خلاصه‌های امنیتی**: خلاصه‌های امنیتی روزانه و هفتگی دریافت کنید تا از وضعیت امنیتی سرور خود مطلع بمانید.

## ⚙️ نصب

برای نصب SSHield، به سادگی دستور زیر را به عنوان کاربر root اجرا کنید:

```bash
bash install.sh
```

نصب‌کننده تمام وابستگی‌ها را مدیریت کرده و اجزای لازم را تنظیم می‌کند.

## 🕹️ استفاده

پس از نصب، می‌توانید منوی SSHield را با تایپ کردن دستور زیر راه‌اندازی کنید:

```bash
sshield
```

این دستور یک منوی CLI تمیز و شهودی را باز می‌کند که در آن می‌توانید به تمام ویژگی‌های SSHield دسترسی داشته باشید. منو به گونه‌ای طراحی شده است که خودآموز باشد و به شما امکان می‌دهد به راحتی در میان گزینه‌های مختلف امنیتی پیمایش کنید.
